@model List<UserDTO>

<div class="container p-4 border">

    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Users</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Auth" asp-action="GetUsers" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i> Create New Product</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Username
                </th>
                <th>
                    Role
                </th>
                <th>
                    Email
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.UserName</td>
                    <td>@item.Role</td>
                    <td>@item.Email</td>
                    <td>
                        <a asp-controller="Auth" asp-action="Update" asp-route-id="@item.Id" class="btn btn-success"><i class="bi bi-pencil-square"></i> Update Product</a>
                        <a asp-controller="Auth" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i> Delete Product</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>